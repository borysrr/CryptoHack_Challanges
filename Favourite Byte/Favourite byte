s = "73626960647f6b206821204f21254f7d694f7624662065622127234f726927756d"

for order in range(256):
    decode = ''.join(chr(b ^ order) for b in bytes.fromhex(s))
    if decode.startswith("crypto"):
        print("Flag:", decode)
        break
